<!--Footer-->
<footer class="bg-white">
	<div class="container px-8 mx-auto">

		<div class="flex flex-col w-full py-6 md:flex-row">

			<div class="flex-1 mb-6 text-black">

				<a href="{{ path('homepage') }}" class="flex justify-center flex-1 mb-4 text-2xl font-bold no-underline transition duration-150 ease-in-out transform-gpu hover:scale-105 hover:no-underline lg:text-4xl">
					{% include 'img/logo.svg' %}
					{#<p>{{ config.get('general/sitename') }}</p>#}
				</a>

				<div class="mr-4">
					<form method="get" action="{{ path('search') }}" enctype="text/plain">
						<input class="w-full px-4 py-2 text-base bg-gray-200 border-2 border-gray-400 rounded-lg focus:outline-none focus:border-indigo-500" value="{{ search|default|escape }}" placeholder="{{ __('listing.placeholder_search') }}" type="search" name="search">
						<button class="px-6 py-3 mx-auto mt-1 font-bold text-white transition duration-150 ease-in-out rounded-full shadow-lg lg:mx-0 gradient transform-gpu hover:scale-105">
							{{ __('action.search') }}
						</button>
					</form>
				</div>
			</div>

			{% include 'partials/_footer_links.twig' %}

		</div>
	</div>

</footer>
